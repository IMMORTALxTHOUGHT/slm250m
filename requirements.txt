# Core training stack (install torch matching the A5000 CUDA driver first)
# NOTE: litgpt 0.5.x requires torch>=2.7, but torch>=2.7 is only built for
# cu128+ which a CUDA 12.4 driver cannot run. So we pin litgpt to 0.4.x and
# pair it with torch 2.6.0+cu124 (the highest cu124 build). See scripts/00_setup.sh.
litgpt>=0.4.0,<0.5
torch>=2.3.0
numpy<2.0
# Data
datasets>=2.19.0
huggingface_hub>=0.23.0
transformers>=4.41.0
tokenizers>=0.19.0
sentencepiece>=0.2.0
# Logging / monitoring
tensorboard>=2.16.0
tqdm>=4.66.0
# Evaluation (installed in eval step; harness pulls its own deps)
# lm-eval>=0.4.2
